# Testing

- Validate changed Python modules with `python -m py_compile`.
- Run the relevant deterministic quality command after changing factory behavior.
- Add focused regression tests for reusable module behavior and configuration validation.
